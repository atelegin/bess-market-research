# Pinned 2026-06 to stop Streamlit Cloud reboots from silently pulling newer
# versions and drifting the rendered chart. Versions match the tested local
# environment. Bump deliberately, then re-verify the published charts.
streamlit==1.55.0
pandas==2.3.3
numpy==2.4.3
scipy==1.17.1
plotly==6.6.0
requests==2.32.5
pyarrow==23.0.1
